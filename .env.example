# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# Left as PLACEHOLDER on purpose: GA only mounts in production builds AND
# when this is set to something other than the literal string "PLACEHOLDER"
# (see components/ConsentBanner.tsx). Also gated by visitor cookie consent.
NEXT_PUBLIC_GA_ID=PLACEHOLDER
